# `docker.sh` (docker cli)
```
udocker pull thangnhox/docker-cli:latest
udocker create --name=docker-cli thangnhox/docker-cli:latest
```

# `docker_ip.sh` (viewing container ip)
```
docker_ip <container_id>
```
